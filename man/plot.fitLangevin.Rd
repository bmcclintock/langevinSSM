% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.fitLangevin.R
\name{plot.fitLangevin}
\alias{plot.fitLangevin}
\title{Plot a fitLangevin object}
\usage{
\method{plot}{fitLangevin}(
  x,
  spatialCovs,
  log = TRUE,
  extent = NULL,
  data = NULL,
  time = NULL,
  compact = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{fitLangevin} object.}

\item{spatialCovs}{List of named \code{\link[terra]{SpatRaster-class}} objects used to fit the model.}

\item{log}{Logical indicating whether to plot the log UD (default) or the probability UD.}

\item{extent}{Optional. A numeric vector of length 4 \code{c(xmin, xmax, ymin, ymax)} or a \code{\link[terra]{SpatExtent}} object defining the bounding box to zoom the plot. If \code{NULL} (default), the extent is automatically calculated from the track data with a 10\% buffer.}

\item{data}{Optional \code{dataLangevin} object. If provided, the observed coordinates will be plotted beneath the estimated locations.}

\item{time}{Optional. Indicates which layer(s) of a dynamic UD to plot. Can be a numeric index, a layer name, or a \code{POSIXct}/\code{Date} object matching the raster's \code{\link[terra]{time}} attribute. If \code{NULL} (default), all UD layers are plotted.}

\item{compact}{Logical indicating whether to plot all tracks on a single panel (\code{TRUE}, default) or plot each track separately (\code{FALSE}).}

\item{...}{Additional arguments (currently ignored, kept for S3 compatibility).}
}
\value{
A \code{\link[ggplot2]{ggplot}} object containing the plot (or a list of \code{\link[ggplot2]{ggplot}} objects if \code{compact = FALSE}), which can be further modified by the user.
}
\description{
Plots the estimated Utilization Distribution (UD) and overlays the estimated true locations (mu) using \code{ggplot2}. If the original data is provided, the observed locations are also plotted for comparison.
}
